name: CI

on:
  push:
    branches:
      - master

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    
    - name: Checkout
      uses: actions/checkout@v1

    - name: Build
      run: |
        npm install
        npm run-script build
      env:
        GITHUBTOKEN: ${{ secrets.GITHUBTOKEN }}

    - name: Deploy
      uses: kpittman23/github-pages-deploy-action@releases/v3
      with:
        GITHUBTOKEN: ${{ secrets.GITHUBTOKEN }}
        BRANCH: gh-pages
        FOLDER: build
